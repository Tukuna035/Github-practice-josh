name: hello

on:
  push:
    branches:
      - main

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello to users
        run: echo "Hello dosto"

      - name: Say hello to user
        run: echo "Hello $USER"
